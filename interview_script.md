# AI应用工程师面试逐字稿

---

## Q1：介绍一下你的Agent项目（1分钟版）

**话术：**

"我做一个检测行业的智能助手，解决同事查规范、查合同、查缺陷效率低的问题。

背景是我们公司有20份工程规范PDF、47份历史合同、599条缺陷记录，分散在不同地方，同事查一个条款要翻十几分钟。

我的方案是搭了一个RAG+Agent系统：先把所有文档切片、向量化，存到Chroma向量库；然后用LangGraph做Agent框架，配了3个工具——查规范、查缺陷、查折扣数据；LLM用DeepSeek，前端Next.js，后端FastAPI，部署在腾讯云服务器上。

现在同事直接问一句'砌体加固要查哪些标准'，Agent自动检索相关规范，返回条款号加原文，还能对比历史缺陷风险。从0到上线用了3周，目前在公司内部使用。"

**追问应对：**
- 问"数据怎么来的？"→ "规范是公开的GB/JGJ标准，合同和缺陷是公司内部数据，脱敏后入库"
- 问"用户反馈怎么样？"→ "同事反馈查条款从20分钟缩短到1分钟，还在收集反馈迭代"

---

## Q2：为什么选RAG不选微调？

**话术：**

"两个原因。一是数据量，我们有117份文档，但总量不算大，微调需要更多数据才能训出效果；二是更新频率，公司每月都有新合同、新规范发布，RAG可以动态更新向量库，微调每次都得重新训练，成本太高。

另外检测行业对准确性要求高，RAG能给出具体的条款号和原文出处，用户可追溯；微调是模型'记住'知识，出处不明确，出了问题不好定位。"

**追问应对：**
- 问"如果数据量大了呢？"→ "可以考虑RAG+微调结合，RAG负责实时检索，微调负责领域语言风格"
- 问"RAG有什么缺点？"→ "检索质量依赖embedding和切片策略，如果chunk切错了，检索会漏掉关键信息"

---

## Q3：Embedding怎么选的？Chunk怎么切的？

**话术：**

"Embedding选的是BAAI的bge-base-zh-v1.5，中文语义理解好，模型小只有400MB，可以本地跑不依赖外网。对比过m3e和text2vec，检测领域术语多，bge效果更好。

Chunk策略是按文档类型差异化处理：规范按'章节+条款'切，保证条款号在同一个chunk里；合同按'项目概述+检测清单'切；报告按'检测方法+结论'切。每个chunk都带metadata，标注来源类型和条款号，方便后续过滤检索。"

**追问应对：**
- 问"chunk多大？"→ "一般200-500 tokens，具体看内容完整性，优先保证语义完整不切断"
- 问"检索效果不好怎么办？"→ "先查是不是chunk切错了，然后加rerank、调metadata过滤、换embedding模型"

---

## Q4：Agent怎么决定调哪个tool？调错了怎么办？

**话术：**

"我们用的LangGraph的ReAct模式，LLM根据用户问题做意图识别，生成思考过程，然后选择对应工具。

三个工具场景很明确：用户问'检测方法/标准'→调知识库检索；问'会出什么质量问题'→调缺陷库；问'折扣/价格'→调折扣统计。我在System Prompt里写清楚了每个工具的触发条件，降低误判。

调错的话，目前靠三层保障：一是prompt约束，二是temperature调低到0.3减少随机性，三是每周抽查20组对话记录badcase，迭代prompt。后续考虑加规则校验层，比如折扣类问题强制走get_pricing_stats不走RAG。"

**追问应对：**
- 问"能举个例子吗？"→ "比如用户问'材料检测多少钱'，Agent可能先调知识库检索费率表，但我们发现RAG抽样不准，所以后来加了规则：只要问折扣/价格，直接走结构化xlsx，不走向量检索"

---

## Q5：部署遇到过什么问题？

**话术：**

"几个问题。一是模型下载慢，服务器在国内，从HuggingFace下bge模型容易超时，后来用hf-mirror.com镜像解决了。

二是环境依赖，bge需要torch+transformers，体积大，我们用venv隔离，模型懒加载，启动时再下，避免容器镜像太大。

三是端口冲突，服务器上已有其他服务占8000，我们改成8001，用nginx反向代理到/inspection/路径。

四是.Chroma数据同步，向量库文件从Windows传到Linux有路径问题，后来打包tar.gz上传解压。"

**追问应对：**
- 问"有监控吗？"→ "有systemd守护进程，自动重启；用journalctl看日志；后续考虑加Prometheus监控API延迟和错误率"
- 问"高并发怎么处理？"→ "目前内部使用并发不高，后续如果对外会加限流和缓存"

---

## Q6：如果给机票业务做Agent，你怎么设计？

**话术：**

"先确认场景，我假设是客服场景。

数据层：接入航班动态API、退改签政策库、用户订单数据，做成可检索的知识库。

工具层：设计4个工具——①查航班状态 ②查退改签政策 ③查用户订单 ④提交改签申请。

Agent层：用户说'我想改签'，Agent先查订单确认航班，再查政策看是否可改签，计算差价，最后引导用户确认。

难点：航班数据实时性要求高，需要轮询或推送；多轮对话要保存上下文状态；用户情绪激动时要有兜底话术转人工。"

**追问应对：**
- 问"实时数据怎么接？"→ "用WebSocket或SSE推送给前端，Agent查数据时走API缓存，5分钟内有效"
- 问"和检测Agent有什么区别？"→ "检测Agent知识库相对静态，机票Agent数据实时变化，对延迟和一致性要求更高"

---

## Q7：你英语怎么样？

**话术：**

"日常工作中经常看英文技术文档，比如HuggingFace的transformers文档、LangChain官方guide、PyTorch API reference。读写没问题，口语方面还在练习，日常工作够用。"

---

## Q8：你有什么问题想问我们？

**建议问：**

1. "团队目前AI这块有几个人？我进去主要负责哪个方向？"
2. "公司目前的LLM是自研还是调API？有没有考虑过私有化部署？"
3. "对我的期望是什么？前3个月主要做什么？"

**不建议问：**
- 加班多吗？（显得怕吃苦）
- 薪资能再涨吗？（等offer阶段再问）
- 公司是做什么的？（显得没做功课）

---

## 面试心态

- 不会的问题："这个方向我目前了解还不够深，但我学习能力强，给我一周可以补上来"
- 被质疑经验少："我项目经验确实只有这一个，但是全链路从0到1，覆盖数据处理、模型选型、开发、部署，对整体流程的理解可能比只做某一环的人更全面"
- 紧张时：深呼吸，放慢语速，停顿2秒思考完全没问题
